<div class="leistungen-grid">
  {% for l in leistungen[:limit|default(99)] %}
    {% if linkTo %}
      <a class="cell" href="{{ linkTo }}#{{ l.id }}">
        <div class="icon-frame"><i class="{{ l.data.icon }}"></i></div>
        <div class="label">{{ l.data.titel }}</div>
      </a>
    {% else %}
      <div class="cell">
        <div class="icon-frame"><i class="{{ l.data.icon }}"></i></div>
        <div class="label">{{ l.data.titel }}</div>
      </div>
    {% endif %}
  {% endfor %}
</div>
